{
  "extends": "./tsconfig.json",
  "include": ["src/**/*"],
  // Capability fixtures are scanner/config input data, not shipped code
  "exclude": ["**/__tests__/**", "src/commands/federation/__fixtures__/**"],
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src",
    "paths": {}
  }
}
